# backpack

A simple command line utility for moving things around.

## commands
- take
- clone
- drop
- inventory

## installation

    ./install

    (will install to /bin)

    or

    ./install install_directory

## usage

    $ take somefile.ext
    $ inventory
    somefile.ext
    $ drop somefile.ext
